# tylt

Containerized pipeline execution engine.

This package is a convenience wrapper around [`@tylt/cli`](https://www.npmjs.com/package/@tylt/cli) for `npx tylt`.

## Quick start

```bash
npx tylt run pipeline.yaml
```

## Install globally

```bash
npm install -g @tylt/cli
```
