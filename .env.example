# Tylt Configuration

# Root directory for all workspace data (relative or absolute path)
# Each workspace contains: {TYLT_WORKDIR}/{workspaceId}/staging/, /artifacts/, state.json
# Default: ./workdir
TYLT_WORKDIR=./workdir
