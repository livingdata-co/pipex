# Pipex Configuration

# Root directory for all workspace data (relative or absolute path)
# Each workspace contains: {PIPEX_WORKDIR}/{workspaceId}/staging/, /artifacts/, state.json
# Default: ./workdir
PIPEX_WORKDIR=./workdir
